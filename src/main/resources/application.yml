spring:
  datasource:
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sell?useSSL=false&serverTimezone=UTC
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    #生产环境指定的两张方式:在默认主配置文件中,打包后用命令行传参
  profiles:
    active: dev
